pipeline {
    agent any
stages {
    stage('Network') {
        steps{
          dir ('env/management/network') {
              sh 'terraform init'
              sh 'terraform plan'
              sh 'terraform validate'
              sh 'terraform plan'
              sh 'terraform apply'
          }
        }
    }
}
}